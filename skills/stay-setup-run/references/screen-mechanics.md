# 화면 동작 — 단계 종류별로 무엇을 누르고 무엇이 일어나는가

`stay-setup-guide` 의 `references/screen-dictionary.md` 가 **칸 이름·선택지·버튼 글자**의 정본이고,
이 문서는 그 화면들이 **어떻게 열리고 어떻게 저장되는지**를 적는다. 값은 지시서(`<이름>_입력지시서.html`)가, 글자는 사전이, 동작은 여기가 정본이다.
러너는 그 지시서만 읽는다 — 원고(`_원고/manual.md`)는 읽지 않고, 지시서에서 뽑은 실행 계획(`steps.json`)은 러너 내부 파일이다.

아래 내용은 호텔 7곳을 1단계부터 끝까지 실행하며 확인하고, **운영 2026-09-04 화면 기준
(2026-09-04 배포판)** 의 템플릿·폼과 한 줄씩 대조해 고친 것이다.

> **운영 2026-09-04 배포판 화면 기준. 그 전 화면에서는 v0.2.x 를 쓴다.**
> 바뀐 것: 새 호텔이 **오퍼 0 · 룸 0** 으로 생기고, 룸 추가 드로어에 룸별
> [오퍼별 표시명] 칸이 생겼고, 프로모션의 `전 오퍼 공통` 카드가 늘 보이며,
> 시즌 드로어의 취소정책 목록이 이 호텔 것으로 좁혀졌고,
> **[오퍼] 탭에 `연령 구간` 패널이 새로 생겼다.**
>
> **이 문서가 대조하지 못한 화면이 하나 있다.** `기본정보` 탭(대표이미지·상품상세 이미지 모달)은
> Stay 바깥의 공통 상품 화면이라 이번 대조 자료에 들어 있지 않다. 아래 `기본정보 탭` 절은
> 실행으로 확인한 내용 그대로 두었다 — `#content-modal` · [이미지 직접등록] 은
> **화면에서 본 것이고 그 밖의 자료로 확인한 것이 아니다.**

## 1. 단계 종류 → 화면 대응표

| 단계 종류 | 탭·화면 | 여는 버튼 | 저장 버튼 |
|---|---|---|---|
| 환율 확인 · 거래처 확인 · 도시 확인 | 호텔 목록 → [호텔 만들기] 폼 | [호텔 만들기](보기만) | **저장 없음** — 지시서의 `→ [목록]` 은 목록으로 돌아오는 줄이다 |
| 호텔 만들기 | 호텔 목록 → [호텔 만들기] 전체 화면 | | [호텔 만들기] — 누르면 편집 화면으로 넘어간다 |
| 기본정보 글 입력 | `기본정보` 탭 (전체 화면) | | 오른쪽 위 [저장] |
| 대표이미지 올리기 · 상품상세 이미지 올리기 | `기본정보` 탭 이미지 블록 | [이미지 직접등록] → 모달, **한 번에 1장** | 모달 헤더 [저장] (장수만큼 반복) |
| 호텔 정보 입력 | `호텔 정보` 탭 (전체 화면) | `추천 포인트` 의 [행 추가] | 탭 안 [저장] |
| 취소정책 만들기 | `취소정책` 탭 | [정책 만들기], [구간 추가] | [추가] |
| 룸 만들기 | `객실` 탭 `객실 (룸 타입)` 카드 | **1번째부터 [룸 추가]**, 안에서 [침대 행 추가] | [저장] |
| 룸 사진 올리기 | `객실` 탭 | 룸 행 [편집] | **저장 버튼 없음** — 지시서는 `→ [사진 추가]` 로 끝나고, 그 버튼이 곧 파일 고르개다 |
| 오퍼 만들기 | `오퍼` 탭 | **1번째부터 [오퍼 추가]** | [저장] |
| ~~오퍼 고치기~~ | — | **러너가 거부한다** — 아래 `오퍼 탭` 절 | — |
| 연령 구간 만들기 | `오퍼` 탭 `연령 구간` 절의 오퍼 카드 | [연령 구간 추가] | [추가] (편집이면 [저장]) |
| 판매 연결 한 번에 만들기 | `객실` 탭 오퍼별 `판매 연결` 카드 | [객실 추가] — **1번째 룸부터 전부** | [추가] |
| 판매 연결 표시명 넣기 · 고치기 | `객실` 탭 판매 연결 행 | [편집] | [저장] |
| 시즌 만들기 | `시즌` 탭 오퍼 카드 | [시즌 추가] | **[추가]** (편집이면 [저장]) |
| 시즌 가격 채우기 | `시즌` 탭 시즌 카드 | [가격] → `가격 일괄 전개` 드로어 | [이 단가로 깔기] |
| 셀 상태 바꾸기 | `시즌` 탭 시즌 카드 | [상태] | [이 상태로 바꾸기] |
| 가격 셀 손으로 고치기 · 만들기 | `가격 캘린더` 탭 | 오퍼 줄 → 달 이동 → 룸 줄(또는 [룸별 입력]) → 날짜 칸 | 그 행의 [저장] · 새 행이면 [추가] |
| 요금제 정본 고치기 · 만들기 | `요금제` 탭 `요금제 정본` 카드 | [편집] · [정본 만들기] | [저장] · [만들기] |
| 요금제 배포 · 전체 적용 | `요금제` 탭 | 객실 행 [요금제 추가] · 정본 행 [전체 적용] | [이 객실에 배포] · [전체 적용] |
| 기준 요금제 바꾸기 | `요금제` 탭 사본 행 [편집] | | [저장] |
| 요금제 삭제 | `요금제` 탭 사본 행 [삭제] | 확인창 — **감독자만** | |
| 판매일 열기 | `판매일` 탭 | [판매일 열기] | [판매일 열기] |
| 부과금 추가 | `부과금` 탭 오퍼 카드 | [부과금 추가], [요율 행 추가] | [추가] |
| 부과금의 연령별 단가 | 같은 부과금 드로어 안 `연령별 단가` 카드 | 없음 — `부과 방식` 을 고르면 선다 | 부과금과 같은 [추가]/[저장] |
| 부가옵션 만들기 · 가격 넣기 | `부가옵션` 탭 | [부가옵션 추가] · 만들어진 카드 [가격 추가] | [추가] |
| 프로모션 추가 | `프로모션` 탭 오퍼 카드 | [프로모션 추가], [단 추가] | [추가] |
| 혜택 추가 · 택1 그룹 만들기 · 그룹에 혜택 추가(= 택1 그룹 후보 추가) | `혜택` 탭 | [혜택 추가] · [택1 그룹 만들기] · 그룹 카드 [이 그룹에 혜택 추가] | [추가] · [만들기] |
| ~~경고 넘어가기~~ | — | **금지된 단계 — 러너가 거부한다**(합격선은 🟡 0) | — |
| 판매 시작 | 검증 배너 | — | **사용자가 누른다** |

## 2. 공통 동작

**탭** — 편집 화면 위쪽 탭 줄의 글자를 누르면 바뀌고 주소 뒤 `#…` 가 바뀐다. 도우미의 범위는 항상
`열린 드로어 → 열린 모달 → 활성 탭 → 문서` 순서로 좁혀진다.

**드로어** — Stay 탭의 입력은 거의 전부 오른쪽에서 밀려 나오는 서랍(`.stay-drawer`, 본문 `#stay_drawer_body`)에서 한다.
표 구조라 **칸 이름은 같은 행의 머리칸**이고, 그 옆 칸에 입력이 있고 그 아래 작은 도움말이 붙는다.
체크박스는 글자를 감싼 라벨이다. 날짜·시각은 고르개 없는 그냥 글자 칸이다
(`2026-01-01`, `14:00`, 오퍼 예약 시작·종료만 `2026-01-01 00:00`).
닫기는 Escape. 열면 주소에 표시가 붙으므로 **드로어를 연 채로 페이지를 새로 고치지 않는다.**

**카드와 행** — 화면은 오퍼별·룸별 카드가 겹겹이다. 도우미가 범위를 좁히는 규칙:

- 제목이 `.stay-card` 상자 안에 있으면 **그 상자가 카드**다. 행이 하나도 없는 빈 카드도 옆 카드까지 넓어지지 않는다.
- `오퍼 · 룸` 두 겹 이름이 표의 행 묶음일 때(요금제 탭)는 **오퍼 제목 뒤의 룸 제목 행부터 다음 룸 제목 행 전까지**가 한 카드다. 두 겹 이름은 표 그룹을 먼저 찾는다 — 앞 조각(오퍼)만 맞는 제목에 먼저 잡히면 안 된다.
- 행 고르기는 **칸 글자 정확 일치 > 앞글자 일치** 순이고, 칸 글자가 이름의 일부일 뿐인 것(`디럭스` 를 찾는데 `그랜드 디럭스` 행)은 세지 않는다.
- ` · ` 조각 구분은 **빈칸이 둘러싼 가운뎃점만** 이다. `Halfboard (조·석식 포함)` 은 한 조각이다.
- 체크 묶음에서 값이 뒤 조각(요금제 이름)만이면 `오퍼 · 룸 · 요금제` 칸 가운데 **이미 켜 둔 대상 룸 이름을 품은 칸을 전부** 켠다.
- 두 조각짜리 칸 이름(`제공 주기 · 1박당 제공 (…)`)은 통째로 못 찾으면 뒤 조각으로 다시 찾는다.
- **같은 이름이 여러 상자에 있으면 그 버튼을 가진 상자가 이긴다.** [오퍼] 탭이 그렇다 — 오퍼
  이름이 목록 행에도, 그 아래 `연령 구간` 카드 머리에도 있다. 목록 행 쪽 글자가 더 정확히 맞아
  (카드 머리에는 상태 표가 붙는다) 이름만으로 고르면 버튼이 없는 행이 잡히고, 그다음 문서 전체
  찾기가 **첫 오퍼의** 버튼을 눌러 버린다. 그래서 `open({button, card})` 은 버튼을 가진 상자를
  먼저 고른다(`stay_helper.js` `narrowCard`). 행을 따로 준 열기에는 이 규칙을 쓰지 않는다.

**저장** — 드로어 안 [저장]/[추가]/[만들기]/[이 객실에 배포]/[판매일 열기]/[이 단가로 깔기] 는 화면을 통째로 다시 그리지 않고
드로어 본문만 바꾼다. 성공하면 드로어가 닫히고 목록이 갱신되며, 실패하면 폼이 오류 문구와 함께 다시 그려진다.
도우미는 이것을 `closed`(성공) / `stayed`(오류 또는 그대로) 로 돌려준다. `stayed` 인데 `errors` 가 비어 있으면 화면을 눈으로 확인한다.
저장이 **성공해도** 안내 띠가 뜬다("저장되었습니다."). 그 글자는 `toast` 로만 오고 `errors` 에는
들어가지 않는다 — 실패로 읽히는 말(실패·오류·없습니다·할 수 없…)이 있을 때만 `errors` 에 얹힌다.
지시서의 저장 글자와 화면 글자가 다르면(시즌 드로어는 [추가]) 도우미가 `저장·추가·만들기·등록·확인` 을 차례로 시도한다.

**검증 배너** — 편집 화면 맨 위(`#stay_validation_banner`). 🔴 묶음(못 팜)과 🟡 묶음(채우면 좋음)으로 나뉜다.
🔴 가 하나라도 있으면 [판매 시작] 은 눌리지 않는다. **합격선은 판매 시작 전 🔴 0 · 🟡 0** 이다 —
🟡 를 사유를 적어 넘기는 `경고 넘어가기` 단계는 **금지**이고 러너가 거부한다. 🟡 가 남으면
지시서 결함이므로 원인을 원고에서 고쳐 지시서를 다시 렌더해 깐다.

**표 액션 칸** — 2026-09-09 부터 목록 표의 액션 버튼들이 `td.td_actions > div.stay-actions` 안에
한 겹 더 싸인다(줄이 접힐 때의 간격을 형제 마진에서 flex `gap` 으로 옮긴 것이다). 버튼의 글자·
순서·`hx-*` 는 그대로라 **버튼을 찾는 길은 바뀌지 않는다** — 다만 "액션 칸의 직계 자식이 버튼"
이라고 가정한 코드는 여기서 깨진다. 러너는 칸 안을 훑어 찾으므로 영향이 없다.

**에이전트 모달 둘(2026-09-09 신설)** — 호텔 목록의 [AI 에이전트로 세팅](`#stay_agent_install`,
탭 셋: `claude`·`codex`·`other`)과 편집 화면의 [에이전트에 복사](`#stay_agent_resume`, 덩어리 둘:
`resume`·`verify`)다. 둘 다 `.stay-modal .stay-agent-modal` 이고 **페이지에 `hidden` 으로 늘 실려
있다** — 여는 버튼은 `[data-agent-open="<루트 id>"]`, 본문은 `[data-agent-text="<키>"]`, 복사는
`[data-agent-copy="<키>"]` 다. 러너는 이 둘을 **열지도 누르지도 않는다**(사람에게 붙여 넣을 문구를
주는 화면이지 입력 대상이 아니다). `hidden` 이라 `state().modal` 에도 잡히지 않는다 — 실행 중
`modal.open` 이 참인데 id 가 이 둘 중 하나면 사람이 손으로 연 것이니 닫고 이어 간다.

**위험 버튼** — 러너가 거부하는 글자는 [삭제]·[그룹 삭제]·[보관]·[보관 해제]·[판매 종료]·[판매 재개]·
[공용으로]·[전용으로 분리]·[세후가로 확정]·[N월 닫기](`1월 닫기` · `2026-01 닫기` 두 꼴) 이다.
글자와 별개로 **`hx-confirm` 이 붙은 버튼은 전부** 거부한다 — 운영 화면에서 확인창이 붙는 버튼은
이 목록보다 넓다(부가옵션·부과금·프로모션·시즌·요금제·룸 사진의 [삭제] 등).

**확인창** — ERP 2026-09-08 이후 `hx-confirm` 은 브라우저 네이티브 confirm 이 아니라 **페이지 안
확인창**을 띄운다: 열려 있는 동안 `<html>` 에 `data-stay-confirm-open="1"` 이 붙고, 루트는
`#stay_confirm`(role=dialog), 문구는 `#stay_confirm_text`, 버튼은
`#stay_confirm button[data-stay-confirm="ok"]` 와 `…="cancel"` 이다. 템플릿의 `hx-confirm` 속성은
그대로 남아 러너의 위험 버튼 거부는 계속 동작한다. `stayRun.submit(글자, {force:true})` 로 누르면
러너가 [확인] 을 대신 누르고 문구를 `confirmText` 로 돌려주며, `stayRun.state().confirm` 이
`{open:true, text}` 로 알려 주고 `stayRun.close()` 는 [취소] 로 먼저 닫는다. 러너 없이 도구가
직접 누를 때는 `[data-stay-confirm="ok"]` 를 클릭한다 — 네이티브 다이얼로그가 아니므로 탭이 멈추지
않는다. 그전 판(네이티브 confirm)에서는 러너가 `window.confirm` 을 대신 답한다.
마스크에도 `data-stay-confirm="mask"` 가 붙어 있는데 **그것은 취소 버튼이 아니다** — 마스크를
**직접** 눌렀을 때만 취소이고(패널 안 클릭이 버블링으로 올라오는 것은 무시한다), 두 버튼은
`button[data-stay-confirm=…]` 이라 태그로 갈린다. `Esc` 도 취소이고 탭은 두 버튼 사이에 갇힌다.
확인창이 열리면 `#stay_confirm` **자신에게도** `data-stay-confirm-open="1"` 이 붙고, 닫히면 그
속성이 `<html>` 과 루트 양쪽에서 사라진다 — 그것이 "처리 끝" 신호다.

**편집 화면 머리의 링크** — 오른쪽 위에 [상세보기]·[이력]·[목록으로]·[저장] 이 있다.
[상세보기] 는 여행사 도메인의 **고객 화면**을 새 탭으로 열고, [목록으로] 는 호텔 목록으로 **떠난다**.
러너는 둘 다 누르지 않는다 — 화면을 떠나면 드로어의 안 저장한 값이 사라지고 부트를 다시 올려야 한다.
머리의 [저장] 은 `기본정보` 탭의 전체 화면 저장 버튼이다(다른 탭의 [저장] 과 같은 글자다).

**세션** — 오래 두면 자동 로그아웃 경고 창(`#logout-warning-modal`)이 뜬다. `stayRun.keepAlive()` 로 [확인] 을 누른다.
이미 만료됐으면 드로어 안에 로그인 폼이 나타난다 → **즉시 중단하고 사용자에게 로그인을 부탁한다.** 비밀번호는 절대 대신 넣지 않는다.

**브라우저 도구에 요구하는 것** — 셋뿐이고, 도구 이름은 상관없다.
① 로그인된 ERP 탭 안에서 **자바스크립트를 실행하고 값을 돌려받기**(`await` 되는 표현식 하나),
② 그 페이지의 **파일 칸에 로컬 파일 넣기**(파일 다리 `input#stay_file_bridge`),
③ **스크린샷**. 셋이 되면 어떤 브라우저 에이전트로도 돈다.

**시간·크기 예산** — 한 호출이 **45초**에서 끊긴다고 보고 단계는 3~10개씩 묶고 단계마다 9~15초 제한을 걸어 누적 30초에서 끊는다.
탭이 뒤에 있으면 크롬이 페이지 타이머를 늦추므로 도우미는 대기를 **워커 타이머**로 잰다(그래서 뒤 탭에서도 동작한다). 스크린샷만 탭이 앞에 있어야 한다.
파일 업로드는 **한 번에 10MB** 까지다. 장당 4~5MB 사진이면 1~3장씩 나눠 올린다.

- **Claude Code (Claude in Chrome 확장) — 실측 완료.** JS 는 `javascript_tool`, 파일은 `file_upload`,
  스크린샷은 `computer`, 탭은 `tabs_context_mcp`·`tabs_create_mcp`·`navigate` 다(전부 `mcp__claude-in-chrome__` 접두).
  확장은 **사이트마다 권한을 따로 받는다** — ERP 도메인을 미리 허용해 둔다. 호텔 7곳을 이 조합으로 끝까지 돌렸다.
- **Codex (Codex Chrome 확장 + 개발자 모드/CDP) — 실측 전.** 된다고 말하지 않는다. ①임의 JS 평가와 반환값,
  ②사이트별 파일 넣기 권한, ③한 호출의 시간 제한 셋을 먼저 확인하고 도구 이름을 적는다.
  파일을 못 넣는 도구면 사진 단계는 `건너뜀 · 사용자 업로드 필요` 로 남기고 이어서 간다.

**진행 표시** — 도우미가 페이지 오른쪽 위에 `div#stay_progress` 띠를 그리고 `runStep` 이 스스로 갱신한다.
`pointer-events:none` 이라 검증 배너나 드로어의 클릭을 가로채지 않고, 화면이 갈려도 스스로 다시 붙는다.
지시서 HTML 을 옆 탭에 열어 두었다면 그 탭에서 `stayGuide.mark([...])` 로 체크를 함께 올린다(없으면 건너뛴다).

## 3. 화면별 메모

### 0단계 확인 (환율 확인 · 거래처 확인 · 도시 확인)
- **저장이 없는 단계**다. [호텔 만들기] 폼을 열어 지시서 값이 그 화면의 목록(공급 통화 셀렉트,
  거래처·도시 자동완성)에 뜨는지만 보고 나온다. 지시서 마지막 줄의 `→ [목록]` 은 저장 버튼이
  아니라 **목록으로 돌아오는 행위**를 가리킨다 — 러너는 이 단계에서 무엇도 저장하지 않는다.
- 러너는 `runStep` 안에서 `runCheckStep` 으로 갈라 `{checked, found, bad}` 를 돌려준다.
  `checked: true` 면 값이 목록에 있다. 값이 목록에 없으면 `bad` 에 담고 **폼에 그대로 남는다** —
  화면을 눈으로 보고 지시서를 고칠 수 있게 두는 것이다(비슷한 값을 대신 고르지 않는다).
- 세 단계와 그다음 `호텔 만들기` 가 **같은 폼**이므로, 다음 단계도 이 폼을 쓰면 [목록] 을 누르지
  않고 그대로 둔다(`stayOnForm: true`). 폼을 떠나는 마지막 단계에서만 [목록] 을 눌러 돌아온다.
- 목록 화면에서 이 단계를 부르면 러너가 [호텔 만들기] 를 눌러 폼으로 간다. 그 이동은 **전체 화면
  이동**이라 JS 컨텍스트가 끊길 수 있다 — 그때는 새 화면에서 부트를 다시 eval 하고 그 단계를 다시 부른다.

### 호텔 만들기 (목록 화면)
- **폼은 늦게 선다**(2026-09-07 운영 실행). 뼈대를 먼저 그리고 공급 통화 목록을 채운 다음
  도시·거래처 select2 를 가장 늦게 붙인다. 그전에 읽거나 채우면 0단계 확인이
  `{checked:false, found:[]}` 로, 호텔 만들기가 칸마다 `not-found` 로 끝난다 — 몇 초 뒤 그대로
  다시 부르면 한 번에 된다. 그래서 **읽기·채우기 앞에서 `await waitHotelForm()` 으로 기다린다**
  (최대 8초 · `{ready, waited}`). 화면으로 옮겨 간 직후에도 한 번 부른다.
  `runStep`(0단계 확인·호텔 만들기)은 이 기다림을 스스로 넣고 `formReady`·`formWaited` 로 알려 준다.
- **이미 폼 위에 있으면 [호텔 만들기] 를 여는 버튼으로 누르지 않는다** — 이 화면의 그 버튼은
  저장 버튼이라 누르는 순간 빈 폼이 저장된다. 0단계 확인 뒤에는 폼에 그대로 서 있으므로 늘 이 경우다.
- 도시·거래처는 **자동완성 검색**이라 글자를 치고 결과를 눌러야 한다(도우미가 검색 칸에 글자를 넣고 keyup 까지 보낸다).
  화면 옵션은 `다낭(DAD)` 꼴이라 지시서의 `푸꾸옥 (PHU QUOC)` 을 통째로 치면 안 걸린다 — 도우미가 전체 → 괄호 앞 → 괄호 안 순으로 시도한다.
- 공급 통화는 **환율이 등록된 통화만** 목록에 뜬다. 없으면 사용자에게 알린다.
- 거래처를 고르면 수수료 네 칸이 저절로 찬다(`자동 입력됨 · 그대로 둠`).
- [호텔 만들기] 는 **전체 화면 이동**이다. JS 호출이 결과를 못 돌려주고 끊길 수 있다 —
  **스크린샷을 한 번 찍어 컨텍스트를 다시 맞추고, 새 편집 화면에서 부트를 다시 eval 한다.**
- **만들어지는 것은 마스터 하나뿐이다**(2026-09-04). 오퍼 0 · 룸 0 · 연결행 0 으로 시작한다 —
  미리 만들어져 고쳐 쓸 오퍼·룸·연결행은 생기지 않는다.
  화면에 서지 않는 필수 레코드(취소요청 설정·거래처 수수료 1행)와 `기본 요금제` **정본** 1행은 그대로 생긴다.
  그래서 **요금제만** 종전대로 새로 만들지 말고 정본을 고쳐 쓴다.
- 만든 직후 점검 배너는 "오퍼가 없습니다" 를 🔴 로 띄운다. 정상이다 — 오퍼를 만들면 사라진다.

### 기본정보 탭
- 이 탭만 다른 위젯을 쓴다 — 셀렉트가 목록을 펼치는 방식(Materialize)이고 자동완성은 select2 다. 도우미가 둘 다 처리한다.
- 저장은 **오른쪽 위 [저장]** 하나이고 **전체 화면 저장**이다. 저장 결과가 `timeout` 으로 보여도 **토스트("저장되었습니다")와 화면 값으로 확인**한다.
- 이미지: [이미지 직접등록] → 모달(`#content-modal`)에 지역·국가·도시 셀렉트, 이미지설명(Alt), PC 이미지 파일 1개, 모바일 이미지 파일 1개, 헤더 [저장].
  **한 번에 한 장**이라 7장이면 7번 반복한다. 대표이미지와 상품상세 이미지는 같은 모양의 버튼이고 올라가는 자리만 다르다.
  모달에는 파일 칸이 둘이라 `takeFiles({index:0, …})` 로 PC 이미지 칸을 지정한다. 미리보기가 만들어질 시간(`wait`)을 주지 않으면 "등록해주세요" 가 뜬다.
  **한 번 열리면 저장 전까지 열려 있다** — 뒤의 [이미지 직접등록] 은 모달에 덮여 눌러도 아무 일이
  일어나지 않는다. 그래서 같은 단계를 다시 부르면 도우미가 다시 누르지 않고 `open: 'already'` 로
  이어간다(러너 0.9.2 · 그 전에는 `open: 'timeout'` 이었다).

### 호텔 정보 탭
- 이 탭도 **전체 화면 저장**이다(탭 안 [저장]). 저장 결과가 `timeout` 으로 보여도 화면 값과 토스트로 확인한다.
- `추천 포인트` 는 [행 추가] 로 늘리는 반복 행이다. 지시서의 `추천 포인트 2 · 제목` 은 두 번째 행이다 — 행이 모자라면 도우미가 [행 추가] 를 먼저 누른다.
- **쌍으로 묶인 행이 있다.** 개장 연도·리노베이션 연도는 머리칸 `연식` 한 줄에, 위도·경도는
  머리칸 `좌표` 한 줄에 나란히 선다. 각 입력 앞에 작은 글자(`위도`·`경도`)가 붙어 있어
  도우미는 그 글자로 칸을 가른다 — 지시서는 `위도`·`경도` 처럼 **작은 글자 쪽**을 쓴다.
  `성급` 은 `연식` 행 **다음**이다.
- 위도·경도는 소수 6자리까지만 저장된다.

### 취소정책 탭
- [정책 만들기] → 드로어. `환불 가능` 을 켰을 때만 `취소 수수료 구간` 이 보인다. 구간은 [구간 추가] 로 늘린다.
- 정책 목록의 [공용으로] 는 **다른 호텔에도 영향을 주는 확인창 버튼**이다. 지시서가 요구해도 감독자 확인 없이 누르지 않는다.
- 같은 규칙의 정책이 이미 있으면 저장이 **막히지 않고 안내로 되돌아온다** — 드로어에
  [있는 것을 보기]·[그래도 새로 만들기] 두 버튼이 붙는다.
  그때 `submit` 은 `stayed` 로 돌아온다. 러너는 **아무 것도 누르지 않고 멈추고** 사용자에게 묻는다 —
  같은 이름의 정책을 하나 더 만들지는 지시서가 정할 일이 아니다.

### 객실 탭
- **1번째 룸도 [룸 추가] 다.** 미리 만들어져 고쳐 쓸 룸 행은 없다(2026-09-04).
- **[룸 추가] 는 룸 타입만 만든다 — 판매 연결행은 만들지 않는다.** 오퍼 select 도 없다.
  판매할 객실이 되려면 아래 `판매 연결` 카드에서 오퍼에 이어야 한다.
- 오퍼가 하나도 없어도 [룸 추가] 드로어는 정상 폼이다(2026-09-10 운영 실측: 오퍼 0 인 호텔에 룸 4개 저장). **"오퍼를 먼저 만드세요"** 막힘 안내와 [오퍼 탭으로 가기] 버튼은 `판매 연결` 카드 쪽에만 뜬다 — 연결은 오퍼 단계를 끝낸 뒤 만든다.
- 객실 목록의 `사진` 열은 올린 뒤에도 0 으로 보이는 화면 결함이 있다(2026-09-10). 사진 수는 룸 드로어의 `룸 사진(N)` 으로 확인한다.
- `룸 이름` 바로 아래에 `룸 설명` 글상자가 있다.
- 침대 구성은 [침대 행 추가] 로 늘리는 반복 행(종류 셀렉트 + 개수). 되읽기가 침대 구성을 못 읽는 일이 잦다 — 화면으로 확인한다.
- **어메니티 카드에는 체크박스 말고 `다른 룸에서 복사…` 셀렉트가 하나 더 있다**(2026-09-07 실측).
  이름 없는 셀렉트고, 첫 항목이 `다른 룸에서 복사…`, 그 뒤는 같은 호텔의 다른 룸 이름이다 —
  고르면 그 룸의 어메니티를 통째로 베껴 온다. **지시서의 칸이 아니다.** 지시서는 어메니티를
  `선택: 욕조, 샤워실` 처럼 체크박스 글자로만 적고, 도우미는 이 셀렉트를 걸러 낸 뒤 체크박스를 켠다.
  (걸러 내기 전에는 `어메니티 | 선택: 욕조` 가 이 셀렉트로 풀려 "`욕조` 옵션이 없습니다 ·
  화면 옵션: 다른 룸에서 복사… / …" 로 끝나고 어메니티가 한 칸도 안 켜졌다.)
- 룸 사진은 드로어 아래쪽 `사진 추가` 파일 칸이고 **여러 장을 한 번에** 넣을 수 있다. 드로어 제목이 대상 룸인지 확인하고 올린다.
- **룸 사진에는 [저장] 이 없다.** [사진 추가] 는 `<button>` 이 아니라 **파일 칸을 감싼 `<label>`**
  이고, 그 파일 칸이 `hx-trigger="change"` 라 **고르는 즉시 올라간다**. 룸 사진 목록
  (`#stay_room_images`)만 제자리에서 갈린다.
  한도는 **최대 20장 · 장당 10MB** 다(패널 안내 문구).
- 그래서 이 단계의 지시서는 `→ [사진 추가]` 로 끝난다(2026-09-04 dict-audit 합의).
  **그 줄은 누를 저장 버튼이 아니라 올리는 행위 자체**를 가리킨다 —
  `stayRun.takeFiles({label:'사진 추가'})` 가 그 줄의 실행이고, 도우미가 요청이 끝날 때까지
  기다린 뒤 `autoUpload: true` · `uploaded` 로 알려 준다.
- **[사진 추가] 를 저장 버튼으로 누르려 하면 도우미가 `upload-label` 로 끊는다.** 끊지 않으면
  러너가 `저장·추가·만들기…` 대안을 훑다가 **룸 폼의 [저장]** 을 눌러, 사진이 붙기 전에 드로어를
  닫는다(실측으로 확인한 사고다). `대표이미지`·`상품상세 이미지` 는 그대로 `→ [저장]` 이다 —
  그쪽은 `기본정보` 탭의 진짜 저장 버튼이다.
- `판매 연결` 카드는 **오퍼마다 하나**다. [객실 추가] 는 그 오퍼에 룸 여러 개를 한꺼번에 잇는다.
  자동 연결행이 없어졌으므로 **1번째 룸부터 전부** 여기서 넣는다.
- 객실이 하나도 없는 오퍼 카드에는 빨간 띠로 **"이 오퍼에 연결된 객실이 없습니다 — …"** 가 선다.
  이것은 **빈 상태 안내문**이지 저장 거부가 아니다 — 오퍼 A 에 룸을 붙여도 오퍼 B 카드의 그 띠는
  화면에 그대로 남는다. 러너 0.9.2 부터 이런 **카드 안의 띠**는 `errors` 가 아니라 `pageNotes` 로만
  간다(그 전에는 룸·판매 연결 단계마다 저장 성공을 실패로 보고했다). 패널 **머리**에 서는 띠는
  진짜 거부라 그대로 `errors` 다.
- 드로어 칸: `오퍼` 셀렉트 하나 + 룸 카테고리 체크박스 여러 개 + **체크박스마다 딸린 [오퍼별 표시명] 입력**
  (2026-09-04) + `새 룸 카테고리명 (선택)`·`새 룸 오퍼별 표시명 (선택)` 한 쌍
  (화면 글자 그대로다).
  그 두 칸은 **추가 드로어에서 늘 보인다**(편집 드로어에서만 `+ 새 룸 카테고리 만들기` 를 골라야 열린다).
  이미 그 오퍼에 연결된 룸은 체크박스도 표시명 칸도 잠긴다(`(이미 연결됨)`).
- **표시명 칸에는 라벨 요소가 없다** — 자리표시 글자가 N 칸 모두 같아 이름만으로는 가릴 수 없다.
  지시서는 `오퍼별 표시명 · <룸 카테고리명>` 으로 적고(그 차례로만 적는다), 도우미가 옆 체크박스
  글자를 이름에 붙여 찾는다(`stay_helper.js` `mateBoxLabel`). 카테고리명은 같은 단계의
  `룸 카테고리` 줄에 적힌 글자와 **한 글자도 다르지 않아야** 한다. 표 차례는 이렇다:

  ```
  | 오퍼 | 선택: <오퍼명> |
  | 룸 카테고리 | 선택: Single, Standard Double, … |
  | 오퍼별 표시명 · Single | 싱글룸 |
  | 오퍼별 표시명 · Standard Double | 스탠다드 더블룸 |
  | 새 룸 카테고리명 (선택) | 비움 |
  | 새 룸 오퍼별 표시명 (선택) | 비움 |
  ```
- 표시명을 이 드로어에서 넣었으면 연결행마다 [편집] 로 다시 넣을 일이 없다. 지시서가 따로
  `판매 연결 표시명 넣기` 단계를 두면 그때는 **행 이름을 `오퍼 · 룸`** 으로 찾는다.
- 연결행을 만들 때 **요금제 사본이 자동으로 배포된다**(`auto_apply` 정본, 없으면 기본 정본).
  그래서 `요금제 고치기` 단계는 그대로 필요하다.

### 오퍼 탭
- **첫 오퍼부터 [오퍼 추가]** 다(2026-09-04).
- **`오퍼 고치기` 라는 단계는 러너가 무조건 거부한다.** 제목이 그것이면 옛 화면의 `기본 오퍼`
  고쳐 쓰기라고 본다 — 이미 만든 오퍼를 되돌아가 고치는 지시서는 내지 않는다는 것이
  `stay-setup-guide` 쪽 규칙이고(그쪽 검사기도 오류로 막는다), 러너도 같게 맞춘다.
  오퍼 값을 바꿔야 하면 원고의 `오퍼 만들기` 단계 값을 고쳐 지시서를 다시 렌더한다.
- 취소정책이 없는 오퍼는 **판매 시작이 막힌다**(2026-09-04: 경고 → 차단). 오퍼마다 기본 취소 정책을 고른다.
- 예약 시작·종료는 `2026-01-01 00:00` 꼴, 투숙 시작·종료는 `2026-01-01` 꼴이다.
- 행 버튼 중 [삭제]·[보관]·[판매 종료]·[판매 재개]·[보관 해제] 는 위험 버튼이다(전부 확인창이 붙는다).

### 오퍼 탭 — `연령 구간` 절 (2026-09-04 신설)
- 오퍼 목록 **아래**에 `연령 구간` 절이 붙는다(`#stay_offer_age_bands`). 오퍼 하나가 카드 하나이고
  카드 제목은 오퍼 이름, 카드 버튼은 **[연령 구간 추가]** 다. 드로어 제목은 `연령 구간 추가 — <오퍼명>`.
- 저장 버튼은 **[추가]**(편집이면 [저장]). 행 버튼은 [편집]·[삭제]이고 [삭제]는 확인창이다.
- 칸은 차례대로 `밴드 코드` · `노출명` · **`최소 연령 (만 나이)`** · **`최대 연령 (만 나이)`** ·
  `방 인원수에 포함`(체크) · `요금 기준 유형`(셀렉트) · `요금 기준 값` · `참조 밴드 코드`(셀렉트) ·
  `상세(자유텍스트)`. 나이 두 칸의 라벨은 2026-09-08 에 `(만 나이)` 를 달았다 — 옛 표기로 적힌
  지시서도 러너는 그대로 받지만, 화면에서 읽히는 글자는 이쪽이다.
  모델의 `정원 산입 여부` 는 **화면에서 `방 인원수에 포함` 으로 덮여 있다** — 지시서는 화면 글자를 쓴다.
- `요금 기준 유형` 은 `성인 요금의 %` / `정액` / `타 밴드 요금과 동일` / `무료` 넷이고,
  **고른 유형에 따라 다음 줄이 서고 접힌다**: %·정액이면 `요금 기준 값`, 타 밴드면 `참조 밴드 코드`,
  무료면 둘 다 없다. 그래서 지시서는 **유형을 값보다 먼저** 적어야 하고, 무료 구간에는 값 줄을 내지 않는다.
  (칸이 늦게 서므로 도우미가 한 번 쉬었다 같은 이름으로 다시 찾는다 — `stay_helper.js` `fill` 의 2차 갈래.)
- `밴드 코드` 는 **영대문자·숫자 2~8자**다(`^[A-Z0-9]{2,8}$`, 예 `CHD`·`INF`·`TEEN`).
  소문자로 쳐도 대문자로 굳는다. **밑줄은 못 쓴다** — 이 코드가 가격 셀 좌표에 그대로 실리는데
  (`A2C1_CHD`) 밑줄이 그 좌표의 조각 구분자다. 파서가 이 꼴을 미리 잡는다
  (`parse_guide.py --check` 의 `값 형식이 어긋난 단계`).
  거부 문구는 `연령 구간 코드는 영대문자·숫자 2~8자여야 합니다(예: CHD · INF · TEEN) — 읽지 못한 값: …` 다.
- **`C` + 숫자 꼴(`C1`·`C12`)은 정규식을 통과해도 따로 거부된다** — 그 모양이 좌표의 아동 조각
  머리와 같아서 `A2C1_C1` 이 "1명 + 1명" 으로 읽히고 왕복이 깨진다. 문구는
  `'C1' 는 인원 조합 키의 아동 조각 표기(C+숫자)와 같아 구간 코드로 쓸 수 없습니다 — CHD · TEEN 처럼 글자로 시작하는 코드를 주세요` 다.
- **좌표 문자열은 32자까지다**(`occupancy_key` 컬럼). 성인 수에 `A` 가 붙어 길어지는 경우까지 세므로
  긴 코드를 여럿 물린 조합(`A2C1_XXXXXXXX_C1_YYYYYYYY`)은 그 길이에서 막힌다 — 코드를 짧게 짓는
  실질적인 이유가 이것이다. 문구는 `인원 조합 키가 너무 깁니다(32자까지) — <키> (<n>자)` 다.
- **좌표 꼴은 `A<성인수>` 뒤에 아동 조각이 붙는 한 가지다.** 아래 넷이 실제로 거부되는 모양이고,
  파서가 `--check` 에서 미리 잡는다(`occupancy_key_shape` — 서버 `parse_key` 와 같은 판정을
  생성 키 14,776개로 맞춰 두었다).

  | 모양 | 왜 안 되나 |
  |---|---|
  | `A2_C1_CHD` | 첫 아동 조각은 성인 자리에 **붙여 쓴다**(`A2C1_CHD`). 철자가 둘이면 유니크 제약이 같은 조합을 두 좌표로 본다 |
  | `A2C1_C1_CHD` | **혼합 금지.** 코드 없는 조각은 하위호환 `A2C1` 한 모양뿐이다. 조각이 둘 이상이면 `A2C1_CHD_C1_INF` 처럼 코드를 다 적는다 |
  | `A2C0_CHD` | 아동 수가 0인 조각은 좌표가 될 수 없다 |
  | `A2CHD` · `A2C1_CHD_XX` | 코드는 **직전 아동 조각의 것**이다. 앞에 조각이 없거나 한 조각에 코드가 둘이면 거부 |
- **나이 경계는 양끝이 포함이다.** 같은 오퍼 안에서 범위가 겹치면 폼이 거부한다 —
  `0~5.99` 와 `6~11.99` 는 괜찮지만 `0~6` 과 `6~11.99` 는 만 6세에서 겹쳐 막힌다
  계약서가 `11.99` 같은 실측 경계를 쓰는 이유다.
  다른 오퍼의 구간끼리는 겹쳐도 된다. 파서가 이 겹침을 미리 잡는다
  (`parse_guide.py --check` 의 `나이 범위가 겹치는 단계`).
- **`요금 기준 유형` 은 이제 계산에 쓰인다**(2026-09-08). 아이가 방에서 자는 1박 요금은
  아래 `시즌 탭` 의 `아동 추가 금액 — <노출명>` 칸 **한 곳**에서 나온다:
  - `무료` → 그 구간은 좌표에 안 실리고 시즌 채우기에 **칸이 서지 않는다**(방값이 성인 칸에서 다 나온다).
  - `정액` → 그 값이 시즌 채우기 칸의 **초깃값**이 된다(시즌마다 고칠 수 있다).
  - `성인 요금의 %` → 시즌 채우기 칸이 **읽기 전용**이다(`성인 1인 추가분의 75% 로 계산됨`).
    구간에 비율이 안 적혀 있으면 그 자리에 `성인 요금의 % 구간인데 비율이 비어 있습니다 —
    연령 구간에서 먼저 채워주세요` 가 대신 뜬다. 여전히 읽기 전용이라 러너는 지나가지만,
    **그 구간의 아동 셀은 안 깔린다** — 이 글자를 보면 `연령 구간` 의 `요금 기준 값` 부터 채운다.
  - `타 밴드 요금과 동일` → 금액 칸이 선다(비우면 그 구간 셀을 안 만든다).
  `부과금` 탭의 `연령별 단가` 는 **방값이 아니라** 의무 갈라디너처럼 따로 붙는 부과금의 연령별
  금액이다 — 두 곳은 다른 축이고, 방값을 거기 적으면 두 번 청구된다.
- **단계 차례**: `연령 구간 만들기` 는 그 오퍼의 첫 `시즌 가격 채우기` 보다 **앞**이어야 한다.
  유료 구간이 하나도 없으면 시즌 채우기 드로어에 아동 금액 칸이 서지 않는다. 파서가 이 차례를
  미리 잡는다(`parse_guide.py --check` 의 `단계 차례가 어긋난 단계`).

### 시즌 탭
- 시즌은 오퍼 카드 안에 있다. [시즌 추가] 로 만들고 **저장 버튼은 [추가]** 다 — 지시서도 `→ [추가]` 로 끝난다.
- 시즌 카드마다 [편집][가격][상태][재전개][삭제] 가 붙는다. 목록 열은
  `시즌 · 우선순위 · 전개 일수 · 구간 · 가격 셀 · 깔린 값 · 점검 · 액션` 이다 —
  전개 결과를 확인할 때 `가격 셀` 수와 `깔린 값` 을 함께 본다.
- **시즌 가격 채우기는 시즌 카드의 [가격]** → `가격 일괄 전개` 드로어다. 저장 버튼은 [이 단가로 깔기].
  칸 차례는 **화면 위에서 아래로** 이렇다(폼이 `order_fields` 로 이 자리에 세운다):
  `판매 단가(공급 통화)` · `정가(취소선, 선택)` · `공급 원가(net, 선택)` ·
  `대상 룸`(체크, 칸 이름이 `오퍼 · 룸`) · `요금제(선택)`(체크, 칸 이름이 `오퍼 · 룸 · 요금제`) ·
  `기준 요금제 대비 조정(선택)` · `인원 조합(선택)` · `인원 조합별 조정(선택)` ·
  **`박수별 단가(선택)`** · **`아동 추가 금액 — <노출명>` × 유료 구간 수** ·
  `이미 값이 있는 날도 덮기` · `이 시즌이 예전에 깐 다른 인원 조합·요금제 셀 지우기`.
  ⚠ 아동 금액 칸은 **박수 칸 뒤·덮기 스위치 앞**이다.
- **`인원 조합(선택)` 은 A-형 좌표만 쓴다** — `A2,A3` · `A2C1_CHD`. 숫자(`2,3,4`)로 적어도 서버가
  받아 `A2,A3,A4` 로 저장하지만, 같은 좌표가 화면·요금표·엔진 로그에서는 A-형으로 다시 나오므로
  지시서가 숫자로 적으면 담당자가 그 두 자리를 잇지 못한다. `인원 조합별 조정(선택)` 의 키도
  A-형이고 **부호가 필수**다(`A3:+14,A4:+26` · `A3:-5%`). 아동 2명처럼 자동 전개로 안 나오는
  조합은 직접 적는다(`A2C2_CHD`). 파서가 옛 숫자 키를 미리 잡는다.
- **`박수별 단가(선택)` (2026-09-08 신설)** — 형식은 `박수:1박 단가` 를 쉼표로: `3:100,5:90`
  = "3박부터 1박 100, 5박부터 1박 90". **차액이 아니라 단가라 부호를 붙이지 않는다.**
  박수는 **2 이상**이다(1박 단가는 위 `판매 단가` 칸이라 `1:…` 은 거부된다). 비우면 종전처럼
  1박 한 층만 깔린다. 층 하나가 셀 수를 통째로 곱한다. `인원 조합`·`아동 추가 금액` 은 각 층에도
  같은 규칙으로 걸리고, 정가·원가는 1박 값 그대로 간다.
  **LOS 절대 단가 계약("3박 이상 100")은 이 칸으로 넣는다** — 가격 셀을 날마다 손으로 열지 않는다.
- **`아동 추가 금액 — <노출명>` (2026-09-08 신설)** — 구분자는 괄호가 아니라 **EM DASH(`—`, U+2014)
  앞뒤 공백**이고 `<노출명>` 은 그 연령 구간의 노출명(없으면 밴드 코드)이다: `아동 추가 금액 — 소아`.
  - **유료 구간마다 한 칸.** `무료` 구간은 칸이 서지 않으므로 지시서에도 줄이 없다.
  - 값은 **아동 1명당 성인 좌표에 더할 금액**(공급 통화)이다. `A2` 좌표에 `A2C1_CHD` = A2 + 그 값이
    **같은 전개에서 함께** 깔린다.
  - **비우면 그 구간의 아동 조합 셀을 안 만든다.** `0` 은 "무료로 깐다" 라 뜻이 다르다.
  - `성인 요금의 %` 구간이면 칸이 `disabled`+`readonly` 다(`성인 1인 추가분의 75% 로 계산됨`).
    지시서는 그 줄을 **`자동 입력됨 · 그대로 둠`** 으로 적고, 러너는 **건드리지 않고 지나간다** —
    값을 넣으려 들면 실패한다.
  - **아이가 방에서 자는 1박 요금의 자리는 이 칸 하나뿐이다.** 부가옵션으로 소아 숙박(쉐어베드·
    소파베드·아동 1박 조식 등 숙박 성격)을 만들지 않는다. 부가옵션은 따로 사는 것만이고
    (식사 업그레이드·픽업), **엑스트라베드는 언제나 부가옵션**이다(방 정원 밖 침대라서다).
- **시즌은 서로 겹치지 않는다**(지시서 v0.5+). 좁은 시즌(Peak)은 넓은 시즌 날짜에서 빼서 적으므로
  `이미 값이 있는 날도 덮기` 는 **늘 해제**다. 체크를 켜는 회차가 지시서에 있으면 실행하지 말고
  지시서 결함으로 보고한다 — 덮기로 고치는 것이 아니라 원고에서 시즌 날짜를 갈라야 한다.
- 지시서의 회차 순서가 곧 결과다. **순서를 바꾸지 않는다.** 전개 결과 셀 수가 0이면 시즌이 겹친
  것이므로(앞 회차가 이미 그 날을 깔았다) 지시서 결함으로 보고한다.
- 날짜 규칙 유형을 바꾸면 보이는 칸이 달라진다(기간 범위 ↔ 월 목록 ↔ 요일 규칙 ↔ 날짜 나열). 유형을 먼저 고른다.

### 요금제 탭
- 정본은 `요금제 정본` 카드 안에 있고, 각 객실 줄 아래에 배포된 사본이 붙는다.
- 배포 대상은 표의 **`오퍼 → 룸` 제목 행 묶음**이다. 카드 범위를 표 그룹으로 잡지 않으면 첫 [요금제 추가] 가 열려 **엉뚱한 룸에 배포**된다.
- **[전체 적용] 은 아직 배포 안 된 객실이 있을 때만** 정본 행에 나타난다. 버튼이 없으면 `건너뜀 · 이미 배포됨`.
- **사본 [편집] 드로어에는 `기준 요금제` 체크 하나뿐**이다. 지시서가 정본 칸(요금제명·코드·포함물…)을 나열하면 러너는 칸 없음으로 멈춘다 — 체크만 켜고 저장하고 지시서 결함으로 보고한다.
- **`지금 모든 객실에 배포` 는 [정본 만들기] 드로어에만 있다.**
  [편집] 으로 연 정본에는 그 체크도 `배포` 머리칸도 없고, 저장 버튼도
  [만들기] 가 아니라 [저장] 이다. 그래서 `요금제 고치기` 단계에 그 줄이 있으면 **지시서 결함**이다 —
  배포는 별개 단계(정본 행의 [전체 적용])다. 러너는 없는 체크 칸을 막지 않고 경고로만 남긴다.
- **`포함물` 반복 행에는 열 제목이 없다.** 두 입력은 자리표시 글자로만 갈린다 —
  왼쪽 `포함물 이름 (예: 조식 2인)`, 오른쪽 `설명 (선택)`. 정본·사본 드로어가 같은 마크업이다.
  행은 `<div>` 하나가 한 행이고 [행 추가] 로 늘린다. 지시서는 `포함물 1 · 포함물 이름` 으로 적는다.

### 가격 캘린더 탭
- 순서: **오퍼 줄 고르기 → 목표 달로 이동 → 룸 줄 고르기(개요 화면이면 [룸별 입력] 으로 전환) → 날짜 칸 클릭 → 인원 조합 행 채우기 → 그 행의 [저장]**.
- 개요 화면은 **판매일이 열린 달**에만 룸 줄을 보여 준다. 달을 먼저 옮긴다.
- 룸 줄은 룸 이름이 아니라 **오퍼별 표시명**으로 보인다. 도우미가 `객실` 탭 판매 연결 표에서 표시명을 찾아 바꿔 쓴다.
- 지시서 카드 표기는 다섯 꼴이다: `오퍼 · 룸 × 요금제 의 인원 조합 N 행` · `… 의 인원 무관 단일가 행` · `… 행` · `오퍼 의 표시명 × 요금제` · `… 의 인원별 · 박수별 가격 추가`. `인원 조합 빈 행` 은 인원 무관이다.
  마지막 꼴의 정본은 **지금 화면에 적힌 이름 그대로** `인원별 · 박수별 가격 추가` 다(2026-09-08 에
  `인원별 가격 추가` 에서 바뀌었다 — 박수 축이 돌아오며 제목이 두 축을 함께 부른다).
  옛 이름 `인원별 가격 추가` 는 **호환 입력**으로만 받는다: 부트(`stay_boot.js`)가 두 표기를 다 읽으므로
  그전에 만든 산출물은 그대로 돌지만, 새 원고는 화면 글자를 쓴다.
- 곱셈 기호는 **앞뒤에 빈칸이 있는 `×`** 만 인정한다(`Deluxe` 의 x 는 아니다).
- [룸별 입력] 전환은 늦게 끝나기도 해서 칩이 보일 때까지 기다렸다 한 번 더 누른다.
- **오퍼 줄은 오퍼가 둘 이상일 때만 그려진다.** 하나뿐이면 계약 이름이 글자로만 선다(누를 것이 없다).
- **오퍼 칩과 룸 칩은 둘 다 `.stay-coord-group` 안에 있다.** 가르는 것은 클래스다 —
  오퍼는 `.stay-offer-btn`, 룸 좌표는 `.stay-coord-btn` 이다.
  상자만 보고 아무 버튼이나 잡으면 오퍼 칩을 룸 칩으로 오인한다.
- 룸 좌표 칩은 **[룸별 입력] 화면에만** 있다. 전체 보기에서는 날짜 칸 안의 미니 행(`.stay-ov-row`)을
  눌러 그 좌표로 갈아탄다.
- 날짜 칸은 판매일이 열린 날만 `button.stay-cal-cell` 이다. 닫힌 날은 `span` 이라 눌리지 않는다.
- **`가격 셀 만들기` 인데 셀이 이미 있으면**(시즌 전개가 이미 깔았다) 새 행 [추가] 가 아니라 기존 행 [저장] 이다.
- 셀 편집창(`#stay_cell_edit`)은 표가 **하나이거나 둘**이다. 그 날짜에 셀이 있으면 기존 행 표 +
  새 행 표 둘이고, **하나도 없으면 새 행 표 하나뿐**이다.
- **2026-09-08 에 이 창이 바뀌었다:**
  - 두 표의 열이 **`인원 조합 · 박수~ · 판매가 · 정가 · 공급 원가 · 상태 · 액션`** 으로 같아졌다 —
    **`출처` 열은 없어졌다**(늘 비어 있었다). 거기 살던 세전 경고는 `판매가` 칸으로 옮겼다.
  - **`박수~` 열이 되살아났다.** "그 박수 **이상**" 이라는 뜻이고(견적은 날짜마다 요청 박수 이하에서
    가장 큰 층을 고르고, 이하 층이 없는 날이 끼면 그 체류는 마감이다 — 최소 층 폴백 없음, 2026-09-09).
    후보는 `<datalist id="stay_los_options">` 로 1·2·3·4·5·7 이
    붙지만 자유 숫자도 받는다(모달 하나에 datalist 하나이고 위아래 표가 같은 id 를 가리킨다).
    박수를 고치는 것은 **다른 좌표로 옮기는 일**이라 그 좌표가 이미
    있으면 저장이 막힌다. 상한은 **30박**이고(`season.MAX_LOS_NIGHTS`) 1 미만은 거부다.
  - **배지는 층 수에 따라 다르다.** 층이 하나뿐인 묶음(실측 78,109행이 이쪽)은 배지가 없고,
    층이 둘 이상인 묶음은 **1박 행에도** `1박~` 배지가 선다 — 층을 나란히 읽는 자리라 바닥 층에
    이름이 없으면 그 줄에서 `1` 이 무엇의 1 인지 안 읽힌다. 그래서 `3박~` 배지만 찾으면 안 된다.
  - 기존 행 표는 **좌표 묶음**으로 그린다: 같은 인원 조합의 여러 박수 층이 한 묶음이고,
    `인원 조합` 칸은 그룹 머리에 **한 번만**(글자, 입력칸 아님) 선다 — **그 열에는 채울 칸이 없다.**
    옛 숫자 키(`2`) 행에는 회색 `옛 형식` 배지가 붙는다.

### 셀 편집 창 — 좌표별 묶음 표의 속살 (2026-09-09 배포판)

자동화가 걸려 넘어지는 자리가 셋이다. 셋 다 **화면이 아무 말도 하지 않는** 종류라 여기 적어 둔다.

1. **층 둘째 행부터는 `<td>` 가 하나 적다.** 그룹 머리(`td.stay-cell-group`)가 `rowspan` 으로 층
   전체를 덮기 때문이다. `rowspan` 은 그 묶음의 **오류 배너 행까지** 덮는다(배너도 제 `<tr>` 이라
   안 덮으면 아래 층이 한 칸씩 밀린다) — 그래서 `rowspan` 값이 층 수보다 클 수 있다.
   자리(`row.children[i]`)로 칸을 세면 `판매가` 에 적을 값이 `정가` 칸에 들어가고 저장까지 간다.
   러너는 `stayRun.rowCells(row)` 로 격자를 풀어 **열 번호**로 잡는다(`colspan` 도 함께 푼다).
   층이 둘 이상인 묶음의 `박수~` 칸에는 `.stay-cell-tier` 가 붙어 한 칸 들여쓴다.
2. **`인원 조합` 값은 행마다 `<input type="hidden" name="occupancy_key">` 로 실린다.** 사람이 읽는
   자리만 그룹 머리로 올라갔을 뿐 값이 없어진 것이 아니다(저장 버튼의 `hx-include` 가 그 행에서
   걷는다). 그래서 좌표를 되읽을 때 **숨은 칸을 빼면 안 된다** — 빼면 좌표가 하나도 안 잡혀
   되읽기가 통째로 빈다. 숨은 칸은 `박수~` 칸 안에 있다(`인원 조합` 칸이 아니다).
3. **저장 거부는 배너 한 줄로 온다.** 이 표는 한 줄이 한 좌표인 컴팩트 표라 필드 오류를 칸 옆에
   두지 않고 그 행 **아래**의 `<tr><td colspan="6">` 에 `.stay-banner--blocking` 하나로 모은다
   (새 행 표는 `colspan="7"`). 클래스에 `error` 가 없어 `[class*=error]` 훑기로는 안 잡힌다 —
   러너는 `.stay-banner--blocking` 을 따로 읽어 `errors` 에 담고 `submit: 'refused'` 로 알린다.
   `--warning`·`--info`·`--ok` 는 담지 않는다(저장이 성공해도 서는 안내다).
   실제로 나오는 문구:
   - `같은 인원 조합의 가격 행이 이미 있습니다 — 위 표에서 그 행을 고치세요`
     (좌표는 `(인원 조합 × 박수)` 둘 다 같을 때만 겹친다 — 문구는 인원만 부르지만 박수도 축이다)
   - `판매 가능한 셀은 0원일 수 없습니다. 값을 넣거나 셀 상태를 '데이터 누락'으로 바꿔주세요`
   - `정가는 판매가보다 작을 수 없습니다`
   - `박수는 1 이상이어야 합니다 — 1이 '박수 무관'입니다` · `박수는 30 이하여야 합니다 — 그보다 긴 계단은 견적이 집지 않습니다`
   - `인원 조합을 읽지 못했습니다 — 목록에서 고르거나 A2(성인 2명) · A2C1_CHD(성인 2명 + CHD 아동 1명) 형식으로 씁니다 …`
   - `이 날짜에 판매일이 없습니다 — 가격을 매달 자리가 없습니다. 판매일을 먼저 여세요.`(창 머리)

**층을 골라 고치려면** 단계의 칸에 `박수~` 를 적는다 — 러너가 그 값의 층을 먼저 찾고, 그 층이
아직 없으면 아래 추가 표로 떨어져 새 좌표를 만든다. `박수~` 를 안 적으면 종전대로 묶음의
**첫 층(1박~)** 이 대상이다. 카드 줄(`… 의 인원 조합 A2 행`)에는 층을 부르는 자리가 없다.
  - 아래 카드 이름이 **`인원별 가격 추가` → `인원별 · 박수별 가격 추가`** 로 바뀌었다.
  - 그 카드의 `인원 조합` 이 **텍스트 → 셀렉트**로 바뀌었고 항목 글자는 **키가 앞**이다:
    `인원 무관 단일가` · `A2 · 성인 2` · `A2C1_CHD · 성인 2 · 소아 1` ·
    `A2C1 · 성인 2 · 아동 1(구간 미지정)` · `3 · 3인`(옛 형식).
    지시서가 값으로 `A2C1_CHD` 만 적어도 도우미가 **앞 조각이 같은 항목**을 고른다
    (`A2` 는 `A2C1_CHD` 의 앞글자이기도 해서 글자 전체로는 갈리지 않는다 — `stay_helper.js`
    `pickOption` 의 앞 조각 갈래). 유료 연령 구간이 있는 오퍼면 아동 조합 항목까지 서고,
    후보가 하나도 없는 계약이면 자유 입력이 그대로 남는다.
  - 그 카드의 `박수~` 는 초깃값 `1`(= 박수 무관)이다. 종전에는 hidden 1 이었다.
- 이 창은 Materialize 모달(`.modal.open`)이 아니라 Stay 의 2층 모달(`.stay-modal`)이다.

### 판매일 탭
- [판매일 열기] 드로어에서 시작일·종료일·요일 체크·대상 룸 체크 → [판매일 열기].
- 시작일이 오늘보다 앞이면 오늘 이후로 옮긴다.
- 월 단위 닫기 버튼은 `<N>월 닫기` 이고(계약이 해를 넘기면 `2026-01 닫기`), 열 머리는
  `월별 판매일 닫기` 다. 전부 확인창 버튼이다. **누르지 않는다.**

### 부과금 탭
- **시간대별 요율 행을 하나라도 추가하면 정액·정률 칸이 사라진다.** 정액/정률을 먼저 채우고 [요율 행 추가] 를 누른다.
- 한 행에 입력이 둘인 칸이 있다(`종류` = 셀렉트+이름, `부과 방식` = 셀렉트 둘, `정액 금액` = 방식+금액).
  라벨이 겹치면 도우미가 `모호` 로 알려 준다 — 라벨 뒤에 `#2` 를 붙인다.
- 정률 값은 되읽기가 못 읽는 일이 있다. 화면으로 확인한다.
- `대상 룸` 후보는 판매 연결이 있어야 뜬다.
- **`부과 방식` 과 `부과 단위` 는 다른 축이다.** 방식(`basis`)은 `정액` / `1박 요금의 %` 이고,
  단위(`per`)는 `체류당 1회` / `박당` / `인당` 이다. 둘을 섞어 적으면 옵션이 없다고 멈춘다.
- **`연령별 단가` 카드(2026-09-04 신설)는 조건부로 선다.** `부과 단위` 가 **인당**이고
  `부과 방식` 이 **정액**이며 **시간대별 요율 행이 0개**일 때만 보인다(세 조건이 다 맞아야 선다).
- **2026-09-08 부터 그 자리가 빈칸으로 남지 않는다.** 앞 두 조건은 맞는데 요율 행이 있으면
  같은 자리에 `연령별 단가` 제목의 **안내 카드**가 서고 입력 표 대신 한 줄이 뜬다:
  `아래 [시간대별 요율] 표에 행이 있어 연령별 단가를 쓸 수 없습니다 — … 요율 행을 모두 지우면
  이 자리에 연령별 단가 표가 다시 나옵니다. …`. 러너에게 이것은 **채울 칸이 없는 카드**다 —
  제목이 있다고 칸이 있는 것이 아니므로, 그 단계가 `not-found` 로 끊기면 값이 아니라
  **줄 차례**를 의심한다(이 카드가 곧 그 진단이다). 카드 표시는 Alpine `x-show` 라 htmx 왕복
  없이 즉시 갈린다 — 요율 행을 한 줄 넣는 순간 표가 사라진다.
- 화면 칸 차례는 이렇다(지시서도 이 차례로 적는다 — 2026-09-04 dict-audit 합의):
  `종류` → `이름` → `부과 방식` → `부과 단위` → `정액 금액`/`정률` → `부과 유형` → `적용 날짜 (선택)` →
  `적용 룸 scope` → **`연령별 단가 · <노출명>`** → `시간대별 요율` → `설명`.
  요율 행을 먼저 넣으면 연령별 단가 카드가 사라진다. 파서가 이 차례를 미리 잡는다
  (`parse_guide.py --check` 의 `줄 차례가 어긋난 단계`).
- **`적용 날짜 (선택)` 은 부과 유형이 `선택` 이면 화면에 서지 않는다.** 지시서가 `비움` 이면
  그것으로 만족이다 — 러너 0.9.2 부터 되읽기가 `(칸 없음)` 을 `absent` 로 적고 통과시킨다
  (그 전에는 `mismatch` 로 저장을 막았다). 날짜 **값**이 적혀 있는데 칸이 없으면 그대로 불일치다.
- 연령별 단가 줄은 그 오퍼의 **연령 구간마다 하나**다. 칸 이름은 구간의 `노출명` 이고(표의 머리칸),
  열 제목은 `1인 단가 (<통화>)` 다. 지시서는 `연령별 단가 · <노출명>` 으로 적는다 —
  도우미가 통째로 못 찾으면 뒤 조각(노출명)으로 다시 찾는다.
- **비움과 0 은 뜻이 다르다.** 비우면 위의 정액 금액(성인가)과 같고, `0` 은 그 연령이 무료다.
  지시서에 `0` 이 적혀 있으면 `0` 을 넣는다 — 비우지 않는다.
- **차례가 어긋나면 서버가 조용히 버리지 않고 막는다.** 저장이
  `stayed` 로 돌아오고 셋 중 하나가 뜬다: "부과 단위가 “인당”이어야 합니다" ·
  "부과 방식이 “정액”일 때만 쓰입니다" · "시간대별 요율이 있으면 … 둘 중 하나만 두세요".
  그 문구가 보이면 값이 틀린 것이 아니라 **줄 차례가 틀린 것**이다 — 지시서 결함으로 보고한다.
- 그 오퍼에 연령 구간이 하나도 없으면 카드에 **입력이 아예 없고** 안내 문구만 선다.
  그때는 `오퍼` 탭의 `연령 구간` 을 먼저 만든다.

### 부가옵션 탭
- [부가옵션 추가] 로 정의를 만들고, 만들어진 카드의 [가격 추가] 로 가격 줄을 넣는다. 두 단계다.
- **같은 이름의 부가옵션이 오퍼마다 따로 있다.** 카드 표기가 `오퍼 의 이름` · `이름 중 오퍼가 X 인 카드` · `이름 — 오퍼 칸이 X 인 카드` 꼴이면 그 오퍼 카드로 좁힌다.
  오퍼 한정이 없으면 첫 카드에 들어가므로 **오퍼를 확인하고 실행**한다.
- `적용 방식` 은 `1회 적용 (박수·날짜와 무관)` / `날짜연동 (박수 따라감 — 엑스트라베드)` / `일자선택 (원하는 날만 — 조식)` 세 가지다.

### 프로모션 탭
- `단(tier)` 은 [단 추가] 로 늘리는 반복 행이다. 지시서의 `단 1 · …` 이 첫 번째 단이다.
- **프로모션 탭의 `전 오퍼 공통` 카드**(오퍼 없는 할인, 모든 오퍼에 붙음)는 **공통 프로모션이 0건이어도 늘 보인다**
  (2026-09-04 — 빈 상태 문구가 대신 뜬다). 보통 카드처럼 그 안의 [프로모션 추가] 를 누르면 된다.
  종전에는 카드가 감춰져서 러너가 [프로모션 추가] 요청의 오퍼 번호를 0 으로 바꿔 여는 우회를 썼다 — 그 우회는 지웠다.
- 오퍼가 하나도 없으면 프로모션 탭은 "오퍼가 없습니다" 안내를 띄운다. 오퍼 단계를 먼저 끝낸다.

### 혜택 탭
- 단독 혜택은 [혜택 추가]. 묶어 고르게 하려면 [택1 그룹 만들기] 로 그룹을 먼저 만들고(저장은 [만들기]) 그룹 카드의 [이 그룹에 혜택 추가].
- `제공 주기` 는 되읽기가 못 읽는 일이 있다.
- [그룹 삭제] 는 확인창 버튼이다.

### 검증 배너
- **합격선은 판매 시작 전 🔴 0 · 🟡 0** 이다. 🟡 를 사유를 적어 넘기는 `경고 넘어가기` 단계는
  **금지**다 — 지시서(v0.5+)는 그 단계를 내지 않고, 지시서에 있으면 러너가 거부한다
  (`parse_guide.py --check` 도 브라우저를 열기 전에 막는다). 🟡 가 남으면 원인을 지시서에서
  고쳐 다시 깐다(시즌 겹침 → 날짜 분리, 취소정책 없음 → 정책 만들기).
- 같은 규칙이 여러 건이면 「같은 문제 N건 ▼」로 접혀 있다. 남은 🟡 를 셀 때는 **펼쳐서** 센다.

### 직원 예약 화면 (러너가 만지지 않는다 — 깔린 값이 드러나는 자리)

이 화면은 세팅이 아니라 **파는** 화면이라 러너는 열지 않는다. 그런데 담당자가 "제대로 깔렸나"
를 확인하는 곳이 여기이므로, 실행 뒤 보고에서 이 글자들을 설명할 수 있어야 한다.

- **아동 나이는 만 나이 0~17세**다(2026-09-08 에 12 → 17 로 넓혔다). 라벨은
  `아동 N 나이 (만 나이)`, 빈 선택지는 `만 나이 선택`, 항목은 `만 7세` 꼴이다. 한국어에서 맨
  `나이` 는 세는 나이로도 읽히는데 계약서의 연령 구간은 전부 만 나이라, 한 살 차이가 소아가와
  성인가를 가른다. **지시서의 `최소 연령`·`최대 연령` 도 만 나이다.**
- **박수 층이 모자라면 그 방이 마감으로 뜬다.** 문구는
  `<N>박은 이 요금제가 파는 최소 숙박일수에 미달합니다 — 가격 셀이 <M>박 이상부터 깔려 있습니다`
  이고, 조회 목록 쪽에는 `PRICE_CELL_UNAVAILABLE: … <N>박 요청에 자격 있는 박수 층이 없습니다`
  로 남는다. **세팅 구멍이 아니라 층 구성의 결과다** — 3박 층만 깔고 1박 층을 안 깔면 1·2박
  손님에게 그 방이 안 보인다. 3박 미만도 파는 계약이면 1박 층을 **함께** 깔아야 한다.
- 요금 줄은 두 축을 나란히 적는다: `<기준 인원>` 뒤에 `· 검색 <검색한 구성>`. 앞은 그 가격이
  계산된 좌표(`A3` 이면 `성인 3`)이고 뒤는 담당자가 검색한 인원이다. 둘이 다른 것은 정상이다
  (3인 기준가를 성인 2 · 아동 1이 쓰는 계약이 있다).
- 접수 직전 `예약 인원` 줄이 아이를 어느 구간으로 셌는지 말한다. 이 글자가 지시서의 연령 구간과
  다르면 구간 경계나 `아동 추가 금액` 을 다시 본다.

## 4. 함정 모음

| 함정 | 어떻게 한다 |
|---|---|
| 옛 지시서: `오퍼 고치기`·`스탠다드` 행의 [편집] | 2026-09-04 뒤 화면에는 그 행이 없다. 러너가 거부한다 — 지시서를 다시 만든다 |
| 1번째 룸을 판매 연결에서 뺌 | 자동 연결행이 없어졌다. **1번째 룸부터 전부** [객실 추가] 로 잇는다 |
| 룸 추가 드로어에서 오퍼를 찾음 | 그 드로어는 룸 타입만 만든다. 오퍼 선택은 `판매 연결` 카드의 [객실 추가] 에 있다 |
| `기본 요금제` 정본을 새로 만듦 | 정본 1행은 지금도 호텔 만들 때 생긴다 — [편집] 로 고쳐 쓴다 |
| 이미지 모달에 여러 장을 한 번에 | 한 장씩 [저장] 을 장수만큼 반복 |
| 시즌 드로어에서 [저장] 을 찾다 실패 | 시즌 드로어의 저장은 [추가] 다 — 지시서도 `→ [추가]` 다 |
| 시즌 가격을 가격 캘린더에서 찾음 | `시즌` 탭 시즌 카드의 [가격] 이다 |
| 좁은 시즌을 얹었는데 0셀 | 시즌이 겹쳤다. 덮기를 켜지 말고 지시서 결함으로 보고 — 시즌 날짜를 갈라야 한다 |
| 요금제가 엉뚱한 룸에 배포됨 | 요금제 탭 카드는 표의 `오퍼 → 룸` 제목 행 묶음이다. 배포 뒤 어느 룸에 붙었는지 확인 |
| 요금제 [전체 적용] 이 없다고 실패 처리 | 이미 배포된 상태다 — 건너뛰고 기록 |
| 부가옵션·프로모션이 첫 오퍼 카드에 들어감 | 카드 표기에 오퍼 한정이 있는지 보고, 없으면 사용자에게 확인 |
| 자동완성이 안 걸림 | 검색 칸에 글자를 넣고 keyup 까지 보내야 한다(도우미가 처리) |
| 확인창 버튼을 눌러 버림 | 도우미가 거부한다. 지시서가 요구하면 감독자가 대상 행을 확인한 뒤 직접 누른다 |
| 전체 화면 저장이 timeout 으로 보임 | 기본정보·호텔 정보·호텔 만들기는 전체 화면이다. 토스트와 화면 값으로 확인 |
| 저장했는데 조용히 실패 | 저장 결과가 `stayed` 인데 오류 문구가 없으면 화면을 눈으로 확인 |
| 칸은 다 찼는데 드로어가 안 닫힘(`submitted:false`) | 되읽기가 다 맞고 드로어가 그대로면 저장이 안 눌린 것이다. `stayRun.submit('<지시서 마지막 줄의 버튼>')` 을 한 번만 더 부른다 — 대개 그때 닫힌다. 그래도 안 닫히면 `bad` 로 보고 스크린샷을 찍어 화면을 본다. 다시 누른 사실은 로그 비고에 적는다 (2026-09-07 오퍼 만들기 드로어 1회 · 재현 안 됨) |
| 자동 로그아웃 | 단계마다 `state()` 를 보고 경고가 뜨면 `keepAlive()` |
| 드로어를 연 채 새로 고침 | 하지 않는다. 닫고(`close()`) 다시 연다 |
| 부과금에서 정액 칸이 사라짐 | 요율 행을 먼저 추가했기 때문이다. 정액·정률 먼저 |
| 사진 업로드가 10MB 에서 막힘 | 1~3장씩 나눠 다리에 올린다 |
| 룸 사진을 올리고 [저장] 을 누름 | 룸 사진은 고르는 즉시 올라간다. [저장] 은 룸 폼을 저장해 드로어를 닫는 버튼이다 |
| 연령별 단가 칸이 화면에 없음 | `부과 단위` 가 `인당` 이 아니거나 `부과 방식` 이 `정액` 이 아니거나 요율 행이 이미 있다. 순서를 지킨다 |
| 연령별 단가를 비워도 0 과 같다고 봄 | 비움 = 성인가, 0 = 무료. 다른 뜻이다 |
| 연령 구간에 `요금 기준 값` 칸이 없음 | `요금 기준 유형` 이 `무료`·`타 밴드 요금과 동일` 이다. 유형을 먼저 고른다 |
| 시즌 채우기에 `아동 추가 금액` 칸이 없음 | 그 오퍼에 **유료** 연령 구간이 아직 없다(무료 구간은 칸이 안 선다). `연령 구간 만들기` 를 먼저 한다 — 파서가 미리 잡는다 |
| `아동 추가 금액` 칸에 값이 안 들어감 | `성인 요금의 %` 구간이라 `disabled`+`readonly` 다. 지시서 값이 `자동 입력됨 · 그대로 둠` 이면 건너뛰는 것이 정상이다 |
| `아동 추가 금액` 을 0 으로 채움 | 비움 = 그 구간 셀을 안 만든다, 0 = 무료로 깐다. 다른 뜻이다 |
| `박수별 단가` 를 차액으로 적음 | 차액이 아니라 **1박 단가**다. 부호를 붙이지 않고, 박수는 2 이상이다(1박은 위 `판매 단가` 칸) |
| `인원 조합` 을 `2,3` 으로 적음 | 서버는 받지만 화면·요금표·로그는 A-형으로 되돌려 준다. `A2,A3` 로 적는다 — 파서가 미리 잡는다 |
| 셀 편집에서 값이 **옆 칸**에 들어감 | 좌표 묶음의 `rowspan` 때문이다(층 둘째 행은 `<td>` 가 하나 적다). 러너 v0.6+ 는 `rowCells` 로 열 번호를 푼다 — 옛 러너면 올려야 한다 |
| 3박 층을 고쳤는데 1박 값이 바뀜 | 단계에 `박수~` 칸이 없어 묶음의 첫 층이 대상이 됐다. `박수~` 를 적으면 그 층을 고른다 |
| 셀 편집 저장이 `errors: []` 인데 값이 안 들어감 | 거부 사유는 `.stay-banner--blocking` 배너 한 줄로만 온다. 러너 v0.6+ 는 `submit:'refused'` 로 알린다 |
| 밴드 코드를 `C1` 로 지음 | 아동 조각 표기와 같아 거부된다. `CHD`·`TEEN` 처럼 글자로 시작하는 코드를 쓴다 |
| 좌표를 `A2_C1_CHD` 로 적음 | 첫 아동 조각은 성인 자리에 붙여 쓴다 — `A2C1_CHD` |
| 좌표를 `A2C1_C1_CHD` 로 적음 | 코드 없는 조각은 `A2C1` 한 모양뿐이다. 조각이 둘 이상이면 `A2C1_CHD_C1_INF` 처럼 코드를 다 적는다 |
| 3박 층만 깔았는데 1·2박 손님에게 방이 안 보임 | 마감이 맞다(최소 숙박일수 미달). 3박 미만도 파는 계약이면 1박 층을 함께 깐다 |
| 아이 나이를 세는 나이로 받음 | 화면·지시서 모두 **만 나이**다(직원 예약 선택지도 `만 7세`). 한 살 차이가 소아가와 성인가를 가른다 |
| 요율 행이 있는데 `연령별 단가` 를 채우려 함 | 그 자리에 표 대신 안내 카드가 선다(채울 칸이 없다). 줄 차례를 고친다 — 둘은 상호 배타다 |
| 가격 캘린더에서 오퍼 칩을 룸 칩으로 오인 | 룸 좌표는 `.stay-coord-btn` 이다. `.stay-coord-group` 은 오퍼 줄도 쓴다 |
| 셀이 없는 날짜에서 `가격 셀 만들기` 가 실패 | 그 날은 표가 하나뿐이고 인원 칸이 셀렉트다. 러너 v0.4.0 에서 고쳤다 |
| JS 호출이 45초에서 끊김 | 한 호출에 3~10 단계, 단계마다 9~15초 제한, 누적 30초 예산 |
| 진행 띠가 화면에서 사라짐 | htmx 가 본문을 갈아 끼운 것이다. 다음 `runStep` 이 다시 붙인다(✕ 로 끈 것이면 안 붙는다) |
| 저장을 눌렀는데 화면이 아무 말도 안 함 | 500·CSRF 403 이다. ERP 는 `htmx.config.noSwap = [204,304,'4xx','5xx']` 이라 오류 응답으로 화면을 갈아끼우지 않는다 — 배너도 토스트도 없다. 러너가 `submit:'error'` 로 잡는다(아래) |

### htmx 이벤트 이름 — 두 벌이다 (2026-09-09)

Stay 화면은 htmx **4.0.0-beta6** 을 싣는다(`common/templates/common/_htmx_stay.html`).
htmx 4 는 이벤트 이름을 **콜론 표기**로 바꿨다 — `htmx:afterSettle` 이 아니라 `htmx:after:settle` 이다.
레거시 3화면(`display/banner/header_diy` · `display/menu` · `management/diy_banner`)은 아직 beta5 를
쓰지만 그쪽도 v4 표기다. 도우미는 **두 표기를 다 듣는다**(htmx 1·2 카멜 이름도 함께).

- 요청 한 건의 차례: `before:request` → `before:response` → `after:request`
  → 400 이상이면 `response:error` → (`noSwap` 이면 여기서 끝) → `after:swap` · `after:settle`
  → 던져지면 `htmx:error` → **언제나** `finally:request`.
- `pending` 을 빼는 자리는 `after:request` 가 **아니라** `finally:request` 다 — fetch 가 던지면
  (네트워크 끊김·중단) `after:request` 는 아예 오지 않는다.
- 오류 detail 의 자리도 다르다: htmx 1·2 는 `detail.xhr.status`, htmx 4 는 `detail.ctx.response.status`.
- 리스너는 `document.body` 가 아니라 **`document`** 에 건다. 코어는 元 element 가 DOM 에서
  떨어져 있으면 이벤트를 `document` 에 쏘는데(`target = on?.isConnected ? on : document`),
  Stay 는 패널이 제 자신을 `outerHTML` 로 갈아끼우는 모양이라 저장 뒤에는 거의 늘 떨어져 있다 —
  `document` 에 직접 쏜 이벤트는 그 **아래**인 `body` 에 영영 닿지 않는다.

이름을 놓치면 증상이 조용하다: `hx.error` 가 영영 null 이라 500·403·네트워크 실패가
`errors: []` · `submit:'settled'` 로 **완료 보고**된다. 시험 픽스처도 v4 이름으로 발화해야 한다 —
카멜로 흉내 내면 러너가 v4 를 안 들어도 시험이 통과한다.
| 지시서 탭의 체크가 안 오름 | 지시서를 옆 탭에 열어 두었는지, 그 탭에서 `stayGuide.mark` 를 불렀는지 본다. 탭이 없으면 건너뛰는 것이 정상이다 |
